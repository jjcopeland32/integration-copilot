# 03 — Spec Engine v1
Import OpenAPI (file/URL). Normalize to internal model. Generate **Blueprint** (scoped endpoints/fields, auth, webhooks, rules). Export Markdown + JSON.
Acceptance: import public spec → blueprint in <60s.
