# 05 — Golden Tests v1
Generate 10 baseline tests (auth, create/capture, refunds, idempotency, invalid currency, webhook signature, retries, timeouts, dispute event). Store suite + results; runner.
Acceptance: run suite vs mock; results stored.
