# 06 — Validator/Trace v1
Proxy/middleware validates req/resp vs schema + rules; emits signed traces to /api/trace. UI renders human-readable diffs and fix hints.
Acceptance: failing request shows readable cause + spec link.
