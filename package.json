{
  "name": "integration-copilot",
  "private": true,
  "packageManager": "pnpm@9.0.0",
  "workspaces": [
    "apps/*",
    "packages/*",
    "tools"
  ],
  "scripts": {
    "dev": "pnpm -C apps/web dev",
    "build:packages": "pnpm -C packages/spec-engine build && pnpm -C packages/mockgen build && pnpm -C packages/validator build && pnpm -C packages/orchestrator build && pnpm -C packages/connectors build",
    "build": "pnpm build:packages",
    "build:all": "pnpm build:packages && pnpm -C apps/web build",
    "lint": "pnpm -r lint",
    "prisma:generate": "pnpm -C apps/web prisma generate",
    "prisma:migrate": "pnpm -C apps/web prisma migrate dev --name init",
    "prisma:studio": "pnpm -C apps/web prisma studio"
  }
}
